import SwiftUI

struct SettingsView: View {
    var body: some View {
        Form {
            Text("Nothing here yet.")
        }
        .formStyle(.grouped)
        .frame(width: 420, height: 300)
    }
}
